// todo
// import { store } from '../../store';
// import { setUsername } from '../HomeSlice';

// describe('actions test', () => {
// 	it('set a new username', () => {
// 		let user = store.getState().user;

// 		expect(user.username).toBe('leiwang0418');
// 		expect(user.isEditing).toBe(true);
// 		store.dispatch(setUsername('lei'));

// 		expect(user.username).toBe('lei');
// 		expect(user.isEditing).toBe(false);
// 	});
// });
