UserMenu

basic UserMenu:
```js
<UserMenu />
```

new label message:
```js static
const label = ({
        id: "userMenu",
        defaultMessage: "orther message"
    });

<UserMenu label={label} />
```
